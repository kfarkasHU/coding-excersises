{
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint --ext .ts",
    "test": "npm run build && node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "merge-sass": "1.0.2",
    "typescript": "4.6.3"
  }
}
